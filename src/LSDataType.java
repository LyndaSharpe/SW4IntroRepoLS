public class LSDataType {
    public static void main(String[] args) {
        // data type
        // object = pet dog, pet type, age, weight, name
        // datatype variable_name = value;

        String petType = "Dog"; // String = text value
        String name = "Arlo"; //Characters longer than one
        int petAge = 6; // int = integer = whole number
        double petWeight = 4.5; //double = fraction

        // call your variables back and print
        System.out.println("The pet type is " + petType);
        System.out.println("The pet name is " + name);
        System.out.println("The pet weight is " + petWeight + " kg and the age is " + petAge + " years");

        // create an object, declare at least 3 variables using different data types and print

    }
}
